Blockus EVM Engine Client

This repository uses `pnpm` as it package manager. Please install it globally first.

```
sudo npm install -g pnpm
```

### Install dependencies

```
pnpm install
```

### Run project

```
pnpm demo
```
